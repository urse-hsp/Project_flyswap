import React from 'react';

interface CurrencyItemType {
  isVisible?: boolean;
  onClose?: () => any;
}

const CurrencyItem: React.FC<CurrencyItemType> = () => {
  return <></>;
};
export default CurrencyItem;
