import React from 'react';

interface tradeBridgeType {
  isVisible?: boolean;
  onClose?: () => any;
}

const TradeBridge: React.FC<tradeBridgeType> = () => {
  return <>TradeBridge</>;
};
export default TradeBridge;
