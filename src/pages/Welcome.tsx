import React from 'react';

/**
 * 每个单独的卡片，为了复用样式抽成了组件
 * @param param0
 * @returns
 */

const Welcome: React.FC = () => {
  return (
    <div>
      <div style={{ height: '2000px' }}>123</div>
    </div>
  );
};

export default Welcome;
