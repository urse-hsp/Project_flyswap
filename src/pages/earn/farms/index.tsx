import React from 'react';

interface earnFarmsType {
  isVisible?: boolean;
  onClose?: () => any;
}

const EarnFarms: React.FC<earnFarmsType> = () => {
  return <>earnFarms</>;
};
export default EarnFarms;
