import React from 'react';

interface EarnpoolsType {
  isVisible?: boolean;
  onClose?: () => any;
}

const Earnpools: React.FC<EarnpoolsType> = () => {
  return <>Earnpools</>;
};
export default Earnpools;
